<div class="page-header">
  <h1>Browse Profiles</h1>
</div>

<% @profiles.each do |profile| %>
<div class="media">
  <% pic= profile.gender=='male' ? 'male.jpg' : 'female.jpg' %>
  <%= link_to image_tag(pic, class: "media-object") , profile, class: 'pull-left' %>
  <div class="media-body">
    <h4 class="media-heading"> <%= profile.name %> / <%= ((DateTime.now.to_date - profile.born).to_i / 365)-1 %> / <%=profile.city%> </h4>
  </div>
</div>
<%end%>
